  cd ndb-cluster
 docker-compose up -d  
 docker ps
 docker exec -it mysql1 mysql -u root -p
password:rootpass123

 create database class;
  use class
create table ty( name varchar(20));
 insert into ty values('shruti');

select *from  ty;
exit

docker stop ndb1
docker exec -it mysql1 mysql -u root -p
password:rootpass123
 use class
 select* from ty;
  insert into ty values('sarthak');
  select* from ty;
